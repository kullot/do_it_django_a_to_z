[tool.poetry]
name = "do-it-django-a-to-z"
version = "0.1.0"
description = ""
authors = ["jinwoo"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
django = "4.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
